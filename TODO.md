# TODO

- Add an approved, provider-supported CAPTCHA completion integration if a legal and reliable option becomes available. Automatic CAPTCHA bypass is intentionally not implemented. Current behavior detects challenges, puts that provider on cooldown, continues through other search providers, and returns a visible-browser handoff when every route is blocked.
- Add iframe and open Shadow DOM traversal to rendered page inspection.
- Add optional persistent browser profiles with explicit user consent.

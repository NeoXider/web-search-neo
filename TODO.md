# TODO

- 1.22, passive analysis (shipped): `secret_scan` (JS secrets, code endpoints, openapi, auth forms), `sarif_to`/`baseline` on the three reports.
- 1.23, bounded active checks (shipped): `active_probe` (the call is the consent; OPTIONS/TRACE/GET in scope, budget, requests_made), plus sourcemaps in `secret_scan`, JWT lifetime in `api_report`, the test_run-login → api_report recipe.
- 1.24 (all zero capabilities cost - params and findings on existing actions): `site`/`hosts` modes for `secret_scan`/`active_probe`; supply-chain summary across reports; GraphQL literals in `code_endpoints`; `test_run` variables (data-matrix); contract diet for one future action; formal field-QA gate (parallel agents against one real site, findings pinned as regressions).
- 1.25: unified `save_to`/`sarif_to`/`baseline` on `perf_report` (+`save_to` on `test_run`); element screenshots (`screenshot {selector}`); SARIF dogfooding in CI.
- Never (without changing what this product is): state-changing methods, payloads/exploitation, brute force, fuzzing, login/CAPTCHA bypass, surface guessing (`/.git`, `/.env`), port scans, off-scope hosts, secret values in any output, silent contract growth.
- Replace the loopback bridge with Chrome Native Messaging. Since 1.15.0 the handshake is a mutual challenge-response in which the token never crosses the socket, so a squatter on 127.0.0.1:8765 learns nothing, and the token file is restricted to the current user. The remaining gap is that the secret is still a file the user account can read, so any process running as the same user can read it and impersonate either side. With Native Messaging Chrome starts the server itself over stdio, no port is listened on at all, and the trust boundary becomes the extension ID Chrome enforces instead of a file on disk. Pipe framing and the native-vs-fallback guard are specced in `docs/native-messaging-design.md`.
- Review the paid CAPTCHA solver integration (`web_search_neo/captcha.py`, 2captcha-compatible API). It is opt-in only: `captcha` `mode='solve'`, or `mode='auto'` with `WEB_SEARCH_NEO_CAPTCHA_AUTO_SOLVE=1`, and always `WEB_SEARCH_NEO_CAPTCHA_KEY`; `auto` otherwise waits for a human, and search `challenge_mode` never calls it (fallback by default, opt-in manual mode for up to three minutes). Open: an official provider-supported completion path if a legal and reliable one becomes available, and per-site policy on where a paid solve is acceptable.
